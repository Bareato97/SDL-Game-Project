#include "entitybuilder.hpp"

Entity& buildWorld(Entity* entityTemplate, const int size, const int numChunks) {

    // TODO build world

    return *entityTemplate;
}

/*
    TODO - NPC builder

    Entity& buildNPC(char* tag, int x, int y, ...) {

        iterates through an enum based on the tag to generate template creaturs

    }

    Entity& buildVillager(char* tag, int x, int y, ...){}
*/